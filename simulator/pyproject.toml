[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-soc-simulator"
version = "0.1.0"
description = "Agentic SOC v5 — Attack Simulator"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "rich>=13.0.0",
]
